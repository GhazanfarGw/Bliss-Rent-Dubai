# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables — never commit real secrets.
# .env.example is intentionally NOT ignored; it has no real values.
.env
.env.*
!.env.example

# Supabase CLI local dev artifacts
supabase/.branches
supabase/.temp

# Phase 9 email template previews — generated by running the test suite
# (supabase/functions/_shared/email/preview.test.ts), local-only, not
# committed.
email-previews

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local scratch/debug artifacts — never intended for the repo
.claude-tmp/
.deploy-bundles-tmp/
dist_old_preverify/
*.tar.gz
_trace*.cjs
*.tmp
vite.config.testdev.ts
